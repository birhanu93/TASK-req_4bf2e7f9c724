services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: workforce
      MYSQL_USER: workforce
      MYSQL_PASSWORD: workforce
    command: --default-authentication-plugin=mysql_native_password
    # Force mysqladmin through TCP (127.0.0.1:3306) so the healthcheck does NOT
    # pass during MySQL's "temporary server" init phase, which only listens on
    # a Unix socket (port=0). If we ping the socket-path here, compose marks
    # the service healthy before real connections on 3306 are possible and
    # dependent services race in and fail with "Connection refused".
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-P", "3306", "--protocol=tcp", "-uroot", "-prootpass"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s
    ports:
      - "3306:3306"
    tmpfs:
      - /var/lib/mysql

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_HOST: db
      DB_USER: workforce
      DB_PASS: workforce
      DB_NAME: workforce
      DB_CONNECT_RETRIES: "30"
      DB_CONNECT_RETRY_MS: "500"
    depends_on:
      db:
        condition: service_healthy
    command: ["php", "bin/console", "migrate"]
    restart: "no"

  seed:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_HOST: db
      DB_USER: workforce
      DB_PASS: workforce
      DB_NAME: workforce
      STORAGE_ROOT: /app/storage
      DEMO_PASSWORD: "Demo!pass-1"
      DB_CONNECT_RETRIES: "30"
      DB_CONNECT_RETRY_MS: "500"
    depends_on:
      migrate:
        condition: service_completed_successfully
    command: ["php", "bin/console", "seed:demo"]
    volumes:
      - ./storage:/app/storage
    restart: "no"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_HOST: db
      DB_USER: workforce
      DB_PASS: workforce
      DB_NAME: workforce
      STORAGE_ROOT: /app/storage
      SESSION_COOKIE_SECURE: "false"
    depends_on:
      seed:
        condition: service_completed_successfully
    ports:
      - "8080:8080"
    command: ["php", "-S", "0.0.0.0:8080", "-t", "public", "public/index.php"]
    volumes:
      - ./storage:/app/storage

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    environment:
      VITE_API_BASE: "http://app:8080"
    depends_on:
      - app
    ports:
      - "5173:5173"

  tests:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_HOST: db
      DB_USER: workforce
      DB_PASS: workforce
      DB_NAME: workforce
    depends_on:
      db:
        condition: service_healthy
    command: ["vendor/bin/phpunit", "--colors=always"]
    volumes:
      - ./coverage:/app/coverage
    profiles:
      - tests
